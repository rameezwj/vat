import { SearchByStringPipe } from './search-by-string.pipe';

describe('SearchByStringPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByStringPipe();
    expect(pipe).toBeTruthy();
  });
});
